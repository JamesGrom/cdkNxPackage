{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "build environment executor",
  "description": "builds the environment variables to be used based on cdk outputs ",
  "type": "object",
  "properties": {
    "local": {
      "type": "boolean",
      "description": "denotes to run local buildenv or not",
      "default": false
    },
    "stackName": {
      "type": "string",
      "description": "name of current stack"
    },
    "cdkOutputsFileName": {
      "type": "string",
      "description": "denotes name of file produced by cdk deploy -O",
      "default": "cdk.outputs.json"
    },
    "pathToTemplateFile": {
      "type": "string",
      "description": "path to local-template.yaml "
    }
  },
  "required": ["stackName"]
}
