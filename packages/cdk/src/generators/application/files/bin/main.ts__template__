#!/usr/bin/env node
import 'source-map-support/register';
import * as cdk from 'aws-cdk-lib';
import { AppStack } from '../lib/app-stack.ts__template__';

const app = new cdk.App();
new AppStack(app, '<%= projectName %>');
